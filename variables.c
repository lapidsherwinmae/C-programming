#include <stdio.h>

int main() 
{
    int a = 3; // declaring out the output using variables
    printf("%d", a); // when calling out the int variables, u need to apply the specifier based on data type and put double qoute and always put comma before calling the variable

    return 0;
}